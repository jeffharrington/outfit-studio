# outfit-studio
An outfit generator and personal database of clothing

## Getting Started

```bash
pnpm install
pnpm dev
```

Open [http://localhost:3000](http://localhost:3000) to view the app.

Local Supabase (Postgres + Storage) is required for development — see `supabase/` and `.env.example`.
